/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  // basePath: '/justbuytravel_next/demo',
  images: {
    unoptimized: true,
  },
  trailingSlash: true,
};

export default nextConfig;
